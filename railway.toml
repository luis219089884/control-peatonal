# Configuración Railway en la raíz del monorepo (backend/ + frontend/)
[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
